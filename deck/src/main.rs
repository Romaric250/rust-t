let point: (i64,i64,i64) = (0,0,0);

let x = point.0;
let y = point.1;
let (u,r,y) = point;


let  y = point.0

let unit:() = ();

// this unit is guaranteed to never hold any value in them. so what is the use of having a zero tuple.


struct Point{
    x:i64;
    y:i64,
    z:i64;
}


fn new_point()

let mut years:[i32, 3] = [1893,434,598];

